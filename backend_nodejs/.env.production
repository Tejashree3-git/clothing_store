# MongoDB Atlas Connection String (replace with your production database)
MONGO_URI=mongodb+srv://leena08m1_db_user:C8FNV6N1MHa35UI3@cluster0.ufs5hd3.mongodb.net/clothing-store?retryWrites=true&w=majority&appName=Cluster0

# JWT Configuration
JWT_SECRET=5dFgH8kP@xY!2025
JWT_EXPIRE=30d

# Server Configuration
PORT=5000
NODE_ENV=production

# Frontend URL (replace with your production frontend URL)
FRONTEND_URL=http://localhost:3000/

# Rate Limiting
RATE_LIMIT_WINDOW_MS=15 * 60 * 1000  # 15 minutes
RATE_LIMIT_MAX=100  # Limit each IP to 100 requests per windowMs

# Logging
LOG_LEVEL=info
LOG_TO_FILE=true

# Security
SESSION_SECRET=your-session-secret-key
COOKIE_SECURE=true
COOKIE_HTTP_ONLY=true

# CORS - Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000/
